# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Xcode build artifacts
ios/build/
ios/DerivedData/
ios/*.xcodeproj/xcuserdata/
ios/*.xcworkspace/xcuserdata/
ios/*.xcworkspace/xcshareddata/swiftpm/
ios/*.xcodeproj/project.xcworkspace/xcuserdata/
ios/Alma.xcodeproj/xcuserdata/
ios/*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# Android (Capacitor generated — re-created by CI)
android/

# Signing / secrets — NEVER commit these
*.jks
*.keystore
*.p12
*.mobileprovision
*.cer

# Fastlane
fastlane/report.xml
fastlane/screenshots/**/*.png
fastlane/test_output/
build/

# Gemfile.lock (optional but saves CI time if committed)
# Gemfile.lock

# Environment variables — NEVER commit real values
.env
.env.local
.env.*.local

# Firebase Functions build artifacts
functions/lib/
functions/node_modules/

# Segundo cerebro / notas Obsidian — conteúdo pessoal, fora do scope do app
segundo-cerebro/

# Backup Xcode project zipado — não deve ficar no repo
Alma.App.Oficial.xcodeproj.zip

# Pastas arquivadas (código descontinuado preservado para referência)
_archive/

# macOS
.DS_Store
**/.DS_Store

# === Credenciais sensíveis (adicionado 2026-04-17) ===
# Apple Developer
*.p8
AuthKey_*.p8
SubscriptionKey_*.p8

# Firebase Admin / Service Accounts
*adminsdk*
*-firebase-adminsdk-*.json
*.iam.gserviceaccount.com.json

# Certificados e senhas em texto
CERTIFICATE_BASE64*
CERTIFICATE_PASSWORD*
distribution.csr

# Pastas inteiras de credenciais (se alguém criar)
Certificados/
Firebase/*.json
Firebase/alma-app-*

# Backup de project files
*.pbxproj.bak
